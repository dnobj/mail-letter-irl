# Legacy tombstone: do not copy this file or place an admin database URL in .env.
# Public admin pages and /api/admin routes are forced off in every environment.
# ADMIN_ENABLED=true causes the public server to fail closed.
#
# The approved local operator runtime is being delivered in slices under issue
# #162. Non-secret configuration will live under %LOCALAPPDATA%/LetterIRL/admin,
# and credentials will be retrieved from an approved vault.
ADMIN_ENABLED=false
